---
export interface Props {
	height: string;
}
const { height = '1.5rem' } = Astro.props;
---

<img style=`height:${height}; display:inline; transform: translateY(.4rem)` src="/favicon.svg" alt="bot" />
