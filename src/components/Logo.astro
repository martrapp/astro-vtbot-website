---
export interface Props {
	height: string;
}
const { height = '1.5rem' } = Astro.props;
---

<img
	style=`height:${height}; border: none; box-shadow: none; display:inline; transform: translateY(.4rem)`
	src="/favicon.svg"
	alt="bot"
/>
